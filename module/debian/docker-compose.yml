version: '3'
services:

  debian:
    build: ctx
    container_name: debian
    hostname: debian
    networks:
      - backend
    tty: true
    environment:
      - TZ=Asia/Shanghai

networks:
  backend:
    external: true
