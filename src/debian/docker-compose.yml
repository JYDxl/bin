version: '3'

services:
  debian:
    image: debian:bullseye-20220316
    container_name: debian
    hostname: debian
    networks:
      - backend
    tty: true
    environment:
      - TZ=Asia/Shanghai

networks:
  backend:
    external: true
