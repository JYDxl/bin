version: '3'

services:
  debian:
    image: debian:bullseye-20221024
    container_name: debian
    hostname: debian
    networks:
      - backend
    tty: true
    environment:
      - TZ=Asia/Shanghai

networks:
  backend:
    external: true
