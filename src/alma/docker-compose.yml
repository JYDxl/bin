version: '3'

services:
  alma:
    image: almalinux:8.5-20220306
    container_name: alma
    hostname: alma
    environment:
      - TZ=Asia/Shanghai
    networks:
      - backend
    tty: true

networks:
  backend:
    external: true
