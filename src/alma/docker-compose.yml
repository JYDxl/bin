version: '3'

services:
  alma:
    image: almalinux:8.7-20221110
    container_name: alma
    hostname: alma
    environment:
      - TZ=Asia/Shanghai
    networks:
      - backend
    tty: true

networks:
  backend:
    external: true
