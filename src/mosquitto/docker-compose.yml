version: '3'
services:

  mosquitto:
    container_name: mosquitto
    build: ctx
    image: mosquitto-node
    networks:
      - backend
    ports:
      - '1883:1883'
    volumes:
      - ./conf/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - /opt/volumes/mosquitto:/mosquitto

networks:
  backend:
    external: true
