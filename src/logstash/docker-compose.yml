version: '3'

services:
  logstash:
    image: bitnami/logstash:8.6.2
    container_name: logstash
    networks:
      - backend
    ports:
      - '8075:8080'
    environment:
      - LOGSTASH_ENABLE_MULTIPLE_PIPELINES=true
      - TZ=Asia/Shanghai
    volumes:
      - /opt/volumes/logstash:/bitnami
      - /opt/volumes/extra:/extra
      - ./config:/bitnami/logstash/config

networks:
  backend:
    external: true
