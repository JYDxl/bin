version: '3'

services:
  rabbitmq-stats:
    image: bitnami/rabbitmq:3.11.8
    container_name: rabbitmq-stats
    networks:
      - backend
    environment:
      - RABBITMQ_USERNAME=root
      - RABBITMQ_PASSWORD=XLrootJYD713
      - TZ=Asia/Shanghai
    ports:
      - '5672:5672'
    volumes:
      - /opt/volumes/rabbitmq:/bitnami

networks:
  backend:
    external: true
