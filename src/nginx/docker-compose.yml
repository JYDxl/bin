version: '3'

services:
  nginx:
    image: bitnami/nginx:1.23.3
    container_name: nginx
    networks:
      - backend
    ports:
      - '80:8080'
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./conf/my_server_block.nginx:/opt/bitnami/nginx/conf/server_blocks/my_server_block.nginx:ro
      - /opt/volumes/nginx:/app

networks:
  backend:
    external: true
