version: '3'

services:
  cerebro:
    image: lmenezes/cerebro:0.9.4
    container_name: cerebro
    environment:
      - TZ=Asia/Shanghai
    ports:
      - '8999:9000'
    networks:
      - backend

networks:
  backend:
    external: true
