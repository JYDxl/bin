version: '3'
services:

  zipkin-node:
    container_name: zipkin-node
    build: ctx
    ports:
      - '9411:9411'
    networks:
      - backend
    environment:
      - STORAGE_TYPE=mysql
      - MYSQL_DB=zipkin
      - MYSQL_USER=root
      - MYSQL_PASS=XLrootJYD713
      - MYSQL_HOST=mysql-master
      - MYSQL_TCP_PORT=3306

networks:
  backend:
    external: true
